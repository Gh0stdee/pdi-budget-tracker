[project]
name = "pdi-budget-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.4.2",
    "pytest-mock>=3.15.0",
    "python-decouple>=3.8",
    "rich>=14.1.0",
    "sqlmodel>=0.0.24",
]
